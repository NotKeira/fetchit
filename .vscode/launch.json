{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug FetchIt",
            "type": "cppdbg",
            "request": "launch",
            "program": "${workspaceFolder}/fetchit",
            "cwd": "${workspaceFolder}",
            "preLaunchTask": "Build FetchIt",
            "MIMode": "gdb"
        }
    ]
}
